/var/log/nginx/*.log {
	monthly
	missingok
	rotate 9000
	compress
	delaycompress
	notifempty
	create
	dateext
	sharedscripts
	postrotate
		docker exec server_nginx_1 sh -c 'kill -USR1 `cat /run/nginx.pid`'
	endscript
}
